<Window x:Class="QuanLyDichVuKhachSan.Views.ServiceDetailDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Chi Tiết Hóa Đơn Dịch Vụ" 
        Height="580" Width="640" 
        WindowStartupLocation="CenterScreen"
        Background="#0F172A"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextHintingMode="Fixed">

    <Grid Margin="20">
        <!-- Main Card Sheet -->
        <Border Background="White" CornerRadius="16" Padding="24" BorderBrush="#CBD5E1" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="6" Opacity="0.25" Color="#000000"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="Auto"/> <!-- Divider -->
                    <RowDefinition Height="Auto"/> <!-- Order Info Box -->
                    <RowDefinition Height="*"/>    <!-- Items DataGrid -->
                    <RowDefinition Height="Auto"/> <!-- Summary / Total -->
                    <RowDefinition Height="Auto"/> <!-- Footer Buttons -->
                </Grid.RowDefinitions>

                <!-- 1. Header -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock x:Name="TxtCategoryTitle" Text="🍲 CHI TIẾT ĐƠN ẨM THỰC &amp; NƯỚC UỐNG" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock x:Name="TxtOrderCodeAndRoom" Text="Mã đơn: DH-AU-007 • Phòng: P.302" FontSize="12" FontWeight="SemiBold" Foreground="#2563EB" Margin="0,2,0,0"/>
                    </StackPanel>

                    <!-- Payment Status Badge -->
                    <Border x:Name="BadgePaymentStatus" Grid.Column="1" Background="#DCFCE7" BorderBrush="#10B981" BorderThickness="1" CornerRadius="8" Padding="10,5" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtPaymentStatus" Text="✓ Đã thanh toán" FontSize="11.5" FontWeight="Bold" Foreground="#059669"/>
                    </Border>
                </Grid>

                <!-- 2. Divider -->
                <Separator Grid.Row="1" Background="#E2E8F0" Margin="0,0,0,10"/>

                <!-- 3. Order Info Details Box -->
                <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="12,8" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Khách hàng -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,4">
                            <TextBlock Text="Khách hàng:" FontSize="10.5" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtCustomerName" Text="Đoàn Minh Trí" FontSize="12.5" FontWeight="Bold" Foreground="#1E293B"/>
                        </StackPanel>

                        <!-- Thời gian lập bill -->
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,4">
                            <TextBlock Text="Thời gian lập bill:" FontSize="10.5" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtBillTime" Text="30/08/2026 14:15:30" FontSize="12" FontWeight="SemiBold" Foreground="#334155"/>
                        </StackPanel>

                        <!-- Nhân viên lập -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,4">
                            <TextBlock Text="Nhân viên phụ trách:" FontSize="10.5" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtStaffName" Text="Trần Thị Thu Hà" FontSize="12" FontWeight="SemiBold" Foreground="#0284C7"/>
                        </StackPanel>

                        <!-- Hình thức thanh toán -->
                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,4">
                            <TextBlock Text="Hình thức thanh toán:" FontSize="10.5" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtPaymentMethod" Text="Tính chung tiền phòng" FontSize="12" FontWeight="SemiBold" Foreground="#D97706"/>
                        </StackPanel>

                        <!-- Ghi chú đơn -->
                        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,2,0,0">
                            <TextBlock Text="Ghi chú:" FontSize="10.5" Foreground="#64748B"/>
                            <TextBlock x:Name="TxtNote" Text="Giao tận phòng nóng hổi" FontSize="11.5" Foreground="#475569" FontStyle="Italic"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 4. Items DataGrid -->
                <DataGrid x:Name="GridDetailItems" Grid.Row="3"
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          CanUserAddRows="False" 
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#F1F5F9"
                          Background="White"
                          BorderThickness="1"
                          BorderBrush="#E2E8F0"
                          RowHeight="36"
                          HeadersVisibility="Column"
                          FontSize="12"
                          Margin="0,0,0,10">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Foreground" Value="#475569"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Height" Value="32"/>
                            <Setter Property="Padding" Value="8,0"/>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT" Binding="{Binding Index}" Width="45"/>
                        <DataGridTextColumn Header="Tên Món / Mục Dịch Vụ" Binding="{Binding ItemName}" Width="*" FontWeight="SemiBold"/>
                        <DataGridTextColumn Header="SL" Binding="{Binding Quantity}" Width="50"/>
                        <DataGridTextColumn Header="Đơn Giá" Binding="{Binding UnitPrice, StringFormat='{}{0:N0} đ'}" Width="100"/>
                        <DataGridTextColumn Header="Thành Tiền" Binding="{Binding TotalPrice, StringFormat='{}{0:N0} đ'}" Width="110" FontWeight="Bold"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 5. Summary / Total -->
                <Border Grid.Row="4" Background="#F1F5F9" CornerRadius="8" Padding="12,8" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="TỔNG TIỀN THANH TOÁN:" FontSize="13" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtGrandTotal" Grid.Column="1" Text="235,000 đ" FontSize="16" FontWeight="Bold" Foreground="#D97706"/>
                    </Grid>
                </Border>

                <!-- 6. Footer Buttons -->
                <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Đóng" Style="{StaticResource PrimaryButton}" Height="34" Width="90" Click="BtnClose_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
