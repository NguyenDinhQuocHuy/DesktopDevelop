<UserControl x:Class="QuanLyDichVuKhachSan.Views.ServicesConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200"
             Loaded="UserControl_Loaded"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType"
             TextOptions.TextHintingMode="Fixed">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="⚙️ QUẢN LÝ TÀI KHOẢN &amp; CẤU HÌNH DỊCH VỤ" FontSize="20" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>

            <!-- Cột trái: Bảng danh sách dịch vụ (Bật / Tắt hoạt động, KHÔNG có nút xóa) -->
            <Border Grid.Column="0" Style="{StaticResource CardPanel}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="📋 DANH SÁCH DỊCH VỤ CỦA KHÁCH SẠN" FontSize="15" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Data.ServicesConfig}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="14" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Icon}" FontSize="15" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="13.5" Foreground="#1E293B" VerticalAlignment="Center"/>
                                                    <Border Background="#E0F2FE" CornerRadius="4" Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Code}" FontSize="10.5" FontWeight="Bold" Foreground="#0369A1"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Text="{Binding ServiceType, StringFormat='{}Loại hình: {0}'}" FontSize="11" Foreground="#0284C7" Margin="0,3,0,0"/>
                                                <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#64748B" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                            </StackPanel>

                                            <!-- Checkbox Bật / Tắt Active -->
                                            <CheckBox Grid.Column="1" IsChecked="{Binding IsActive}" VerticalAlignment="Center" Margin="12,0,0,0"
                                                      Content="Hoạt Động" FontWeight="Bold" FontSize="12" Foreground="#15803D"
                                                      Command="{Binding DataContext.ToggleActiveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                      CommandParameter="{Binding}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Cột phải: Quản Trị Tài Khoản Người Dùng (Thế chỗ Thêm Dịch Vụ Mới) -->
            <Border Grid.Column="2" Style="{StaticResource CardPanel}" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Title -->
                        <RowDefinition Height="*"/>    <!-- Users DataGrid -->
                        <RowDefinition Height="Auto"/> <!-- Form Add / Edit -->
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="👑 QUẢN TRỊ TÀI KHOẢN HỆ THỐNG" FontSize="15" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,12"/>

                    <!-- Danh Sách Tài Khoản DataGrid -->
                    <Border Grid.Row="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="8" Margin="0,0,0,12">
                        <DataGrid x:Name="DgUsers"
                                  AutoGenerateColumns="False" 
                                  IsReadOnly="True" 
                                  CanUserAddRows="False"
                                  RowHeight="38"
                                  SelectionChanged="DgUsers_SelectionChanged"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  HorizontalGridLinesBrush="#F1F5F9">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#F8FAFC"/>
                                    <Setter Property="Foreground" Value="#475569"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Height" Value="32"/>
                                    <Setter Property="Padding" Value="6,0"/>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Tên Đăng Nhập" Binding="{Binding Username}" Width="115" FontWeight="Bold"/>
                                <DataGridTextColumn Header="Mật Khẩu" Binding="{Binding Password}" Width="95" Foreground="#D97706"/>
                                <DataGridTextColumn Header="Họ Và Tên" Binding="{Binding FullName}" Width="*"/>
                                <DataGridTextColumn Header="Vai Trò" Binding="{Binding RoleDisplayName}" Width="85" FontWeight="SemiBold"/>
                                
                                <!-- Trạng thái -->
                                <DataGridTemplateColumn Header="Trạng Thái" Width="95">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding IsActive, Converter={StaticResource BoolToGreenRedBg}}" CornerRadius="6" Padding="6,2" HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding IsActive, Converter={StaticResource BoolToActiveText}}" FontSize="11" FontWeight="Bold" Foreground="{Binding IsActive, Converter={StaticResource BoolToGreenRedFg}}"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!-- Nút Khóa/Mở -->
                                <DataGridTemplateColumn Header="" Width="75">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="{Binding IsActive, Converter={StaticResource BoolToLockBtnText}}" 
                                                    Background="{Binding IsActive, Converter={StaticResource BoolToLockBtnBg}}" 
                                                    Click="BtnToggleStatus_Click" 
                                                    Visibility="{Binding CanBeLocked, Converter={StaticResource BoolToVis}}"
                                                    Style="{StaticResource PrimaryButton}" 
                                                    Height="26" 
                                                    Width="58"
                                                    FontSize="10.5" 
                                                    FontWeight="Bold"
                                                    HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- Form Thêm / Cập nhật tài khoản -->
                    <Border Grid.Row="2" Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="12">
                        <StackPanel>
                            <TextBlock x:Name="TxtFormTitle" Text="➕ THÊM / CẬP NHẬT TÀI KHOẢN" FontSize="12.5" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,8"/>
                            
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="1.2*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Username -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Tên Đăng Nhập (*):" FontSize="10.5" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,2"/>
                                    <TextBox x:Name="TxtUsername" Style="{StaticResource ModernTextBox}" Height="32"/>
                                </StackPanel>

                                <!-- Password -->
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Mật Khẩu (*):" FontSize="10.5" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,2"/>
                                    <TextBox x:Name="TxtPassword" Style="{StaticResource ModernTextBox}" Height="32"/>
                                </StackPanel>

                                <!-- Full Name -->
                                <StackPanel Grid.Column="4">
                                    <TextBlock Text="Họ Và Tên:" FontSize="10.5" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,2"/>
                                    <TextBox x:Name="TxtFullName" Style="{StaticResource ModernTextBox}" Height="32"/>
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Role -->
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Phân Quyền Vai Trò:" FontSize="10.5" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,2"/>
                                    <ComboBox x:Name="CboRole" Height="32">
                                        <ComboBoxItem Content="👑 Admin (Quản trị)" Tag="Admin"/>
                                        <ComboBoxItem Content="🛎️ Lễ Tân (Trực ca)" Tag="Receptionist" IsSelected="True"/>
                                    </ComboBox>
                                </StackPanel>

                                <!-- IsActive -->
                                <CheckBox x:Name="ChkIsActive" Grid.Column="2" Content="Kích hoạt đăng nhập" 
                                          FontWeight="SemiBold" FontSize="11" IsChecked="True" 
                                          VerticalAlignment="Bottom" Margin="0,0,0,8"/>

                                <!-- Action Buttons -->
                                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,0,0,2">
                                    <Button x:Name="BtnReset" Content="Hủy / Mới" Click="BtnReset_Click" Style="{StaticResource OutlineButton}" Height="32" Width="80" Margin="0,0,6,0" FontSize="11"/>
                                    <Button x:Name="BtnSave" Content="💾 Thêm Tài Khoản" Click="BtnSave_Click" Style="{StaticResource PrimaryButton}" Height="32" Padding="12,0" FontSize="11" FontWeight="Bold"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
